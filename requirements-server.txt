# Pinned with ~= (compatible release) — allows patch updates (e.g. 0.135.x)
# but blocks minor/major bumps that could introduce breaking changes or vulns.
# To update: check changelogs, then bump the version here and rebuild.
fastapi~=0.135.1
python-multipart~=0.0.22
uvicorn[standard]~=0.41.0
httpx~=0.28.1
python-dotenv~=1.2.2
bcrypt~=4.2.0
pyotp~=2.9.0
itsdangerous~=2.2.0
# weasyprint 68.x has CVE-2026-49452 (CSS injection via presentational
# hints) — NOT applicable here: pdf_generator.py renders the user's own
# story HTML and never enables presentational_hints. No fixed release yet;
# re-check on the next weasyprint bump.
weasyprint~=68.1
# >=48.0.1: wheels before that statically link an OpenSSL with a June-2026
# security advisory (GHSA-537c-gmf6-5ccf).
cryptography~=48.0.1

# gallery-dl powers the X/Twitter poll path. It is GPL-2.0 and is invoked ONLY
# as a separate subprocess (never imported — see clients/tw/gallerydl.py), so it
# stays at arm's length from PawPoller's MIT code (mere aggregation). Unlike the
# other pins, gallery-dl gets a looser minor-range so it can keep tracking X's
# frequently-changing internal API — that ability IS the reason we depend on it.
# If gallery-dl is missing at runtime, X polling falls back to the built-in
# GraphQL scrape, so this dependency is an upgrade, not a hard requirement.
gallery-dl~=1.32

# Test dependencies (needed by CI test job — not runtime)
# pytest >=9.0.3: 8.x/9.0.0-9.0.2 have PYSEC-2026-1845 (predictable
# /tmp/pytest-of-* dirs, local-user DoS on UNIX). Full suite verified
# green on 9.0.3 (353 passed).
pytest~=9.0.3
pytest-asyncio~=1.3
respx~=0.22

# Explicit pin: the watermark-on-export path (posting/watermark.py) needs PIL.
# Previously only transitive via weasyprint; pin it so it can't vanish.
Pillow>=9.0
