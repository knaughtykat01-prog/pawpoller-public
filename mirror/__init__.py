"""Server → desktop mirroring.

Stage 1 of ``docs/specs/desktop_server_mirroring.md``. See ``mirror.core`` for
the reasoning; this package deliberately holds no FastAPI imports so the logic
stays testable without a running app.
"""
